# Urban Jungle — Lowest-Price Quote Estimator (v2)
# Pinned to the major.minor versions the notebook was developed on.
# Patch versions left open so security fixes are not blocked.

numpy>=2.0,<2.3
pandas>=2.2,<2.4
scipy>=1.13,<1.16
scikit-learn>=1.6,<1.8
matplotlib>=3.10,<3.12
seaborn>=0.13,<0.15
joblib>=1.4,<1.6
shap>=0.46,<0.50            # SHAP explainability (installed on-the-fly if missing)
jupyter>=1.1                # only needed for local Jupyter runs
ipykernel>=6.29             # only needed for local Jupyter runs

# ── Interactive demo (app.py) ───────────────────────────────────────────────
streamlit>=1.36,<2.0        # web app framework
plotly>=5.22,<6.0           # interactive charts in the app
